import 'package:flutter/material.dart';
import 'package:schedule_app/shared/navbar.dart';

class EventList extends StatelessWidget {
  const EventList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
